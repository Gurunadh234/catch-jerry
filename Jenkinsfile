pipeline {
  agent any
  stages {
    stage("Build") {
      steps {
	echo "application build stage"
      }
    }
    stage("Build Image") {
      steps {
	echo "docker image build stage"
      }
    }
    
  }
}
